import React from 'react';

export default function NewCompetition() {

// --- Minimalist Styles ---
const inputContainer = {
  backgroundColor: '#fff',
  padding: '15px',
  borderRadius: '15px',
  marginBottom: '15px'
};

const clickableRow = {
  backgroundColor: '#fff',
  padding: '15px',
  borderRadius: '15px',
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  marginBottom: '15px',
  cursor: 'pointer'
};

const competitorBox = {
  backgroundColor: '#fff',
  padding: '40px 20px',
  borderRadius: '20px',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  marginBottom: '15px'
};

const addCircleBtn = {
  width: '40px',
  height: '40px',
  borderRadius: '50%',
  border: '1px solid #ccc',
  backgroundColor: '#fff',
  fontSize: '24px',
  cursor: 'pointer'
};

const footerButtons = {
  display: 'flex',
  gap: '10px',
  padding: '15px',
  backgroundColor: '#f5f0ff'
};

const btnCancel = {
  flex: 1,
  padding: '15px',
  borderRadius: '15px',
  backgroundColor: '#777',
  color: '#fff',
  border: 'none'
};

const btnSave = {
  flex: 1,
  padding: '15px',
  borderRadius: '15px',
  backgroundColor: '#000',
  color: '#fff',
  border: 'none'
}

  return (
    <div className="subpage">
      <h1>empty for some reason</h1>
    </div>
  );

}  